[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "open-finance-banking"
version = "0.1.0"
description = "Hermes Agent Skill — query bank accounts via Open Finance Brasil (Pluggy)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "pluggy-sdk",
    "requests",
]